spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/UmaruChan23/cloud_test.git
          search-paths: cloud_config/src/main/resources/config
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
          default-label: master
server:
  port: 8071